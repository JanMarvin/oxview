# Start (or reuse) the local oxview preview server

Called automatically by `ox_view_*()`. Exposed for advance use, e.g.
warming up the server before the first preview.

## Usage

``` r
ox_start_server()
```
